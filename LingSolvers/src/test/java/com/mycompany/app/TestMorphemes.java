package com.mycompany.app;

import static org.junit.jupiter.api.Assertions.assertTrue;

import org.junit.jupiter.api.Test;
import java.util.Set;
public class TestMorphemes{
    
    // // @Test
    // public void testSmallData(){
    //     String[] words={"dogs","cats","car","plants"};
    //     //File file = new File("mt1.txt");
    //     DataSet data= new DataSet(words);
    //     Solver solver = new MSolver(data);

    //     Set<Morpheme> output = solver.getMorphemes();

    //     assertTrue(output.contains(new Morpheme("s")));
    //     assertTrue(output.contains(new Morpheme("c")));
    //     assertTrue(output.contains(new Morpheme("dog")));
    //     assertTrue(output.contains(new Morpheme("at")));
    //     assertTrue(output.contains(new Morpheme("plant")));
    //     assertTrue(output.contains(new Morpheme("ar")));
    // }
}